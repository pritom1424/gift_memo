import 'package:flutter/material.dart';

class AddUpdateWidget extends StatelessWidget {
  const AddUpdateWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
